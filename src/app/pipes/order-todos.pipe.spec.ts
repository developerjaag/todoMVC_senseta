import { OrderTodosPipe } from './order-todos.pipe';

describe('OrderTodosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderTodosPipe();
    expect(pipe).toBeTruthy();
  });
});
